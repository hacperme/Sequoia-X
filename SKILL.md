# Sequoia-X — 开发运维与研究 Skill（仓库自含版）

> 供任何 agent / 新会话拿到本仓库即可继续工作。维护者：hacper（fork 自 sngyai/Sequoia-X）。
> 本文件随项目演进持续更新；Hermes 侧完整版见 skill `sequoia-x-reporting`（含 references 全档）。

## 1. 定位与现状（2026-09-04）

A 股日频量化选股 + 日报推送系统，经两轮深度增强后已成**状态感知的组合决策研究平台**：
「全市场扫描出信号 → 可信回测 → 组合模拟（退出/止损/仓位）→ 市场风格过滤 → 风险预算 → 日报」。

**能力链全貌**：
```
事件研究回测(backtest.py, 33s/1y 向量化)         —— 信号胜率/收益/CI/MAE，理想+可成交双口径
组合模拟器(portfolio.py)                          —— 退出引擎×3 + 仓位模型 + 净值/回撤/超额
市场风格(regime.py)                               —— 沪深300 → up/down×low/high 四态
策略状态机(strategy_map.py)                       —— regime→主攻/回避映射 + 信号过滤
风险预算(portfolio --risk-budget)                 —— 状态→总仓位上限
日报(report.py + cron 0fccfa4dd1f7 交易日20:00)   —— TOP10+共振+回测+🌡️市场状态区块
```

**核心研究结论（1y/2y 实证，net 25bp 成本，含退市股防幸存者偏差）**：
- 无全天候策略。**regime 决定一切**：down_high(恐慌)→超跌反抽类最强（上升跌停 5日胜率86.9%/20日均+9.58%、高窄旗形+8.93%）；up_low(慢牛)→趋势主场（海龟/均线/RPS 20日 52%+/+2.5%+）；up_high(情绪顶)→追高类全灭（上升跌停 -4.30%）
- 全状态合并统计是平均化假象——先分 regime 看，再谈结论
- 组合级 A/B/C 验证（2y 联合4策略）：不过滤 +11.9% → regime过滤 +26.3% → +风险预算 26.1% 但回撤 -16.5→-12.5→**-9.2%**、胜率 45.3→49.7→50.6%
- 吊灯止损(chandelier, peak-3×ATR14) 优于固定-8%止损优于纯时间
- 信号质量排序只在容量受限时有效（max_pos=10: 随机-6.6% vs 排序+0.1%）
- 多策略简单联合 = 负 alpha 污染正 alpha（先淘汰负 alpha 策略再谈合并）

## 2. 架构速览

```
main.py                   日常入口：sync增量(8进程) + 7策略 + 飞书
sequoia_x/
  core/config.py          pydantic-settings(.env)  STRATEGY_WEBHOOK_* 路由
  data/engine.py          SQLite + baostock；sync(重试3次)/backfill/退市股回填
  data/em_fetcher.py      东财直连【仅手动低频】⚠️IP限流狠+复权基准不同
  strategy/*.py           7策略(BaseStrategy)：海龟/均线放量/高窄旗形/涨停洗盘/上升跌停/RPS/定增
  market_rules.py         分板涨跌停(主板10/双创20/北交30, 容差0.98)——实盘+回测共用
  backtest.py             向量化事件研究：compute_events/_future_ret/_exec_ret/_stats(CI)/_mae_stats/_portfolio/_run_grid
  portfolio.py            组合模拟器：PortfolioSim(退出×3/仓位/风险预算) + quality评分 + combined + regime-filter
  regime.py               沪深300(本地 index_daily)→趋势MA200×波动率→四态；asof防未来
  strategy_map.py         REGIME_ADVICE 映射表 + apply_regime_filter + regime_markdown
  report.py               ReportBuilder：ST/次新滤除 + 市值50-800亿 + TOP10 + 共振 + 🌡️regime区块
```

## 3. 常用命令（venv .venv/bin/python）

```bash
# 日报（wrapper 已封装全部；单跑 report）
python -m sequoia_x.report --markdown --json-out data/daily_report.json
# 回测（理想+可成交双口径 + 成本/CI/MAE/组合）
python -m sequoia_x.backtest --period 1y --json-out data/backtest_1y.json
python -m sequoia_x.backtest --grid            # 参数网格
# 组合模拟（退出/仓位/质量/状态机/预算可组合）
python -m sequoia_x.portfolio --strategy 高窄旗形 --exit chandelier --max-pos 100 --daily-k 10 \
    --quality --combined --regime-filter --risk-budget --json-out data/pf.json
# 市场状态（日报会内嵌，也可手查）
python -m sequoia_x.regime   # 若加 CLI
python -c "from sequoia_x.regime import get_market_states; ..."
# 全量回填 / 退市股回填（首次/补数据）
python main.py --backfill
python -c "from sequoia_x.core.config import get_settings; from sequoia_x.data.engine import DataEngine; DataEngine(get_settings()).backfill_delisted()"
```

## 4. 数据源与红线（2026-09-04 实测）

- **baostock 主源**：免费、稳定（实测 20 连查 0 失败）；sync worker 已加 3 次重试+重连（勿回退！原无重试=单次抖动丢当日数据）
- akshare `stock_zh_a_hist` **不可用**：ut token 被东财风控断连（RemoteDisconnected）
- 东财 API 直连去 ut 可通，但 **IP 动态限流极狠**（~4-5 请求/min 即 ban >4min）→ 不做自动备源
- 腾讯 fqkline 稳定但无成交额
- 🔴 **红线：三源后复权基准不同**（茅台 09-03：baostock 9961 vs 腾讯 9113 vs 东财 8132）——**跨源混库=价格尺度污染**，策略/回测全错。成交额各源一致。
- baostock 无北交所（库 5215 只全沪深，双创占 38%）

## 5. 回测口径约定（勿混）

- **理想口径** `{h}`：信号日收盘判定 → 次日收盘买 → N日收盘卖；net=毛-成本25bp；gross_* 保留
- **可成交口径** `{h}_exec`：入场日收盘触涨停→剔除(entry_blocked)；退出触跌停→顺延≤5日(exit_extended)
- panel = 现役 UNION 退市表（stock_daily_delisted，101只/24635行）+ 分板涨跌停列 + prev_close
- 同股 20 交易日冷却去重；RPS 横截面指标单独实现（每日 rank≥90 + 收盘≥120日高×0.9）
- ⚠️ 实盘与回测都 shift(1) 防未来函数（RPS roll_high 原含当日=未来函数，已修）

## 6. 状态机与预算参数（strategy_map.py REGIME_ADVICE / PortfolioSim.RISK_BUDGET）

> ⚠️ 本表与代码同步（2026-09-17 核对；此前表内仍把高窄旗形列为多状态主攻，**已过期**——高窄在 aeb5251 修复后已从所有状态主攻移除）

| regime | 主攻 | 回避 | 风险预算(最大仓位) |
|--------|------|------|------|
| up_low 低波慢牛 | 海龟突破/均线放量/RPS 突破 | 上升跌停/涨停洗盘 | 100% |
| up_high 情绪顶 | （无） | 上升跌停/涨停洗盘/海龟突破 | 40% |
| down_high 恐慌 | 上升跌停/涨停洗盘 | 海龟突破 | 60% |
| down_low 阴跌 | （无） | 海龟突破/RPS 突破 | 30%（观望） |
| na 未知 | — | — | 50% |

### 6.1 研究结论复核（2026-09-17，1Y+2Y 重跑）

**信号量与收益严重错配（结构性发现）**：1Y 共 30122 信号，其中 **海龟 60.1% + 均线放量 29.2% = 89.1%**，而这两个策略在多数状态为负期望；**唯一 5/10 日双正期望的 RPS 只占 7.6%**。含义：**"多加策略"边际收益低，瓶颈在信号质量分布而非策略数量**。

**状态机复核结果（2Y，20日 net）**：
- up_low ✅ 吻合：海龟 +2.63% / 均线 +2.49% / RPS +4.30%；回避项 涨停洗盘 +0.01% / 上升跌停 -1.74%
- down_high ✅ 吻合：上升跌停 **+8.74%**（胜率 81.8%）/ 涨停洗盘 +4.88%；海龟 -1.03%
- up_high ⚠️ focus 空正确（该状态 2Y 六个策略**全部为负** -1.04%~-3.55%，且占 43% 交易日=最高频），但 1Y 的 RPS 为 +1.35% → **两期不一致，故不改状态机**
- down_low ⚠️ 证据薄弱：事件全部落在最近 20 交易日，1Y/2Y 是**同一批样本非独立验证**（157 个事件，20 日 count=0）；方向（观望）无误但不可当强结论

**RPS 阈值 90→95（两期同向，已落地 2026-09-17）**：

事件研究口径（net 25bp）：

| 口径 | 阈值 | n | 5日胜率 | 5日均值 | 10日胜率 | 10日均值 |
|---|---|---|---|---|---|---|
| 1Y | 90 | 2349 | 46.0% | +0.20 | 47.4% | +1.07 |
| 1Y | **95** | 1275 | **48.4%** | **+0.57** | **50.0%** | **+2.11** |
| 2Y | 90 | 8033 | 43.6% | -0.08 | 43.6% | +0.14 |
| 2Y | **95** | 4355 | **44.6%** | **+0.08** | 43.8% | **+0.45** |

分状态提升更明显（10日 net）：1Y up_low 46.6%/+1.75% → **55.0%/+3.37%**（胜率 +8.4pp）；1Y up_high +1.35% → +2.47%。

**组合层 A/B 对照（chandelier / max_pos=100 / daily_k=10 / hold=20 / quality+combined+regime+budget 同参数，仅换阈值）—— 幅度远超事件研究**：

| 组 | 净收益 | 年化 | 最大回撤 | 超额 | 笔数 | 胜率 |
|---|---|---|---|---|---|---|
| 1Y RPS90 | **-16.3%** | -16.3% | -21.2% | -15.5% | 392 | 33.2% |
| 1Y RPS**95** | **-0.6%** | -0.6% | **-9.7%** | +0.2% | 282 | **42.2%** |
| 2Y RPS90 | -7.5% | -3.8% | -19.5% | -48.1% | 1434 | 38.4% |
| 2Y RPS**95** | **+0.4%** | +0.2% | **-10.9%** | -40.2% | 1306 | 39.1% |

1Y 净收益 +15.7pp、回撤改善 11.5pp、胜率 +9.0pp；2Y +7.9pp、回撤改善 8.6pp。**组合层改善幅度远大于事件研究** —— 因资金约束下多出的低质量信号会挤占仓位（与"质量排序只在容量受限时有效"互为印证）。

⚠️ **注意：RPS 单策略组合层绝对收益为负（-0.6%）与事件研究 10 日 +1.07% 不矛盾但需留意** —— 组合层叠加了 chandelier 出场、max_pos/daily_k 容量约束、regime 过滤与风险预算，且默认 hold=20d 对 RPS 过长（见下）。

**持仓周期应按策略分档（2026-09-21 用 5/10/20/30/40 日全扫描重做，结论已修订）**：`portfolio.py DEFAULT_HOLD=20` 但 `backtest.py` portfolio 块**固定 h=10**（两处不一致，日报「回测参考」走的是 10 日）。

⚠️ **重做前先修了一个记账 bug（见下节）**，旧结论「RPS 是短周期策略（10 日）、持有 20 日浪费」**仅基于单一 1Y 窗口且长持有期行被 bug 污染，现予修正**。干净口径（chandelier / max_pos=100 / daily_k=10 / quality+regime+budget / net 25bp，数据窗口 1y=2025-09-22~2026-09-21、2y 同末端）两期均值：

| 策略 | h5 | h10 | h20 | h30 | h40 | 两期最优点 | 判读 |
|---|---|---|---|---|---|---|---|
| RPS 突破 | -2.65 | -0.10 | +2.55 | **+8.80** | +7.95 | h30 | 逐笔均收益两期一致随持有期升到 30 日（1y 2.04%/2y 2.05% vs h10 1.39%/0.06%）→ **10 日偏短，宜 20~30** |
| 海龟突破 | -1.80 | +1.30 | **+6.85** | +6.40 | +6.05 | h20 | 两期同向、改进最扎实：h10→h20 净收益 1y -3.4→+0.2、2y +6.0→+13.5 |
| 均线放量 | -11.30 | -11.05 | -6.00 | -6.10 | +0.90 | h40 | 两期多数为负且 h40 两期相反（1y -6.3 / 2y +8.1）→ 维持「不实盘」，不进分档表 |
| 上升跌停 | +1.40 | +1.05 | +1.15 | +1.80 | **+3.15** | h40 | 两期同向偏长持，但仅 62~89 笔 → 证据偏弱，按 20~40 观察 |
| 涨停洗盘 | +1.15 | +1.60 | **+2.20** | +1.70 | +1.60 | h20 | 两期 h20 最优但幅度小（1.1/3.3）、仅 60~73 笔 → 弱证据 |

**结论**：单一持有期对任何一组策略都不合适——海龟/涨停洗盘约 20 日、RPS 20~30 日、上升跌停偏 40 日；只有 RPS 在 1Y 窗口内 h10 略微领先（3.1 vs 2.0，属窗口噪声）。

**✅ 已落地（2026-09-21）：per-strategy hold 单一声明**——`StrategySpec.hold_days`（海龟/均线/高窄/涨停洗盘 20、上升跌停 40、RPS 30，`DEFAULT_HOLD_DAYS=20` 回退），三处引用：
- `backtest.py::run_backtest` 组合层按策略取档（原硬编码 10 日），`--portfolio-hold` 可统一覆盖；
- `portfolio.py::run_portfolio(hold_days=None)` 默认按策略取档（`--hold-days` 缺省即分档），**`--combined` 联合组合取参与策略的最大值**；note 里打印实际档位；
- `tracker.py` 离场提示按**信号来源策略**定档：`register()` 把 `cross_hits[i].strategies` 换算成 `batch["holds"] = {code: hold}`（同股多策略命中取最大），`_hold_for_code()` 逐股取档，`EXIT_HOLD_DAYS` 降级为**老批次回退值并 10→20**；`report()` 与 `scripts/sequoia_report.sh` 第 7 段同步改成按行档位（`r["hold_days"]`），口径提示写「按信号来源策略」——**改 tracker 口径必须同步脚本摘要，否则两条口径再次不一致**。
- 可见变化：老批次（2026-09-21 前，状态无 holds）一律回退 20 日 → 当日「到期」提醒从 24 只（旧全局 10 日）变为 0 只；2026-09-21 那批（pending）已按当日 cross_hits 回填档位（24 只 20 日 / 20 只 30 日）。
- 回归：`tests/test_hold_by_strategy.py`（注册表档位、portfolio 默认分档/显式覆盖/combined 取最大、tracker 逐股定档与回退、报告按行渲染），套件 **33 passed**。

⚠️ **口径提醒**：`avg_hold_days` 是 `(exit_date - entry_date).days` = **自然日**（如 hold=10 交易日 → 均持 14.1），与 `hold_days`（交易日）不同量纲，报告里并列易误读。另 `PortfolioSim.alloc` 用**初始本金**固定定额（capital/max_pos），净值缩水后仓位相对变大 + 现金不足跳过激增，故**跨持有期的净值比较应同时看逐笔均收益**（不受定额影响）。

### 退出规则再评估：时间到期 vs 事件信号（2026-09-22，用户问「是不是不该到期就撤、该等明显信号」）

**口径**：5 变体 × 5 单元 × {1y,2y} 共 44 组 + regime 条件化 12 组 + RPS 全量窗口 4 组（max_pos=100/daily_k=10/quality+regime+budget/net 25bp，数据 2024-01-02~2026-09-21）。变体：A 纯时钟（无止损）/ B 时钟+吊灯K3（**生产口径，复现扫描表**）/ C 纯事件（无时钟上限）/ D 纯事件吊灯K2 / E 上限60+吊灯K3；R1 上涨态关吊灯 / R2 仅 up_low 关吊灯。

**① 系统已经是「事件为主」**：B 口径下吊灯承担大部分退出——联合 77~78%、RPS 66~70%、海龟 38~44%、上升跌停 39~45%、涨停洗盘 30~33%。**所以「等信号才撤」在结构上早已成立，问题只是时钟上限要不要留。**

**② 取消时钟上限（C）不划算**：相对 B，海龟 −0.9/−0.5、涨停洗盘 −0.4/−0.9、上升跌停 0.0/−0.3 **两期同向更差**；只有 RPS 2y(+14.3)、联合 2y(+3.3) 更好，而这两者在 1y 反而更差 → 时钟上限是有效的资金回收机制，不该取消。

**③ 完全不设止损（A）在长窗口更优，但样本偏多头**：RPS 净收益 A vs B = 1y **−2.3 vs +2.5**（A 输）/ 2y **+27.2 vs +15.1**（A 赢）/ all(2.7y) **+25.7 vs +7.3**（A 赢）；A 的逐笔均收益 3.05%、胜率 41.3%（n=1145）明显更干净。**但样本 82~97% 交易日是上涨态**（近一年 down_high 11% / down_low 7%，前一年 down 仅 2%）→ A 的优势等于「没买保险」，会在大熊市反噬；**1y 恰是唯一含 18% 下跌态的窗口，也恰是唯一 A 输给 B 的窗口**。

**④ 唯一两期同向的硬结论：别把止损收紧**。D（吊灯 K=2）在 1y/2y 的 4/4 策略上均不优于 B/C（涨停洗盘 −1.7/−2.7、上升跌停 −1.2/−1.6 最明显），胜率被压到 30~38%——与 2026-09-18「固定 −8% 止损是陷阱」同源。

**⑤ 可落地方向 = 按市场状态切换止损（R2）**：仅 up_low（低波慢牛）关闭吊灯 → RPS 1y 持平(−0.2)、2y **+5.6**、all **+5.5**（三窗口从不变差）；而 R1（上涨态全关，含 up_high）1y −6.3 → **up_high 情绪顶必须保留止损**。⚠️ 仍需更多窗口验证（库内数据仅到 2024-01，无真正熊市样本）→ **未落地，仅作候选**。

**⑥ 海龟对退出规则不敏感**（1y 全变体 −0.7~+0.4 噪声带；2y 13.0~13.8，除 E 10.3）；**小样本策略不可判**（涨停洗盘 n=60~73、上升跌停 n=51~89，差异 0.2~0.9pp）。

**结论**：保持「吊灯 + 分档时钟上限」双条件（= 已落地的 tracker 形态），**不要改成只看信号、不要收紧 K**；tracker 侧可在 up_low 态只提示到期、不提示吊灯以减噪。交付：`/opt/data/deliverables/退出规则评估_Sequoia_20260922.xlsx`（全量结果/事件占比/regime 条件化/结论 4 sheet）。

### 🐛 P0 记账 bug：已持仓股票被重复建仓（2026-09-21 发现并修复，commit 560bed6 本地）

- **现象**：RPS/2y 在 hold=30/40 输出 -85.9% / -81.9%，但逐笔胜率 32%/29%、均亏仅 -14.65%，与净值跌幅量级差约 13 倍。
- **根因**：`PortfolioSim._try_buy` 缺「已持有该股则跳过」守卫。`hold_days` 超过信号冷却期（20 交易日）时，同股第二个信号在旧仓仍持有期间到达 → `self.positions[sym] = {...}` 直接覆盖 → 旧仓 shares/cost 凭空消失且 trades 少记一笔。
- **证据（修复前）**：RPS/2y/h30 成功买入 **857** 次 vs 平仓记录 **756** 笔、「已持仓又买」**211** 次；现金流 `1,000,000 - 6,709,606 + 5,850,892 = 141,286` 与实际期末现金一致（现金按覆盖后的仓位算，旧仓成本已丢）。
- **影响面**：仅 `hold_days > 20` 的组合回测；hold ≤ 20 各配置 `skipped_held=0` → **此前 hold=10/20 的研究结论未被污染**。
- **数据修订**：RPS 2y h30 -85.9→**+15.1**、h40 -81.9→**+14.3**；RPS 1y h30 -15.2→+2.5、h40 -11.0→+1.6；海龟 2y h30 6.4→12.3、h40 6.8→10.2；均线 2y h30 -6.9→-2.0、h40 4.8→8.1。
- **修复 + 回归**：加守卫与 `stats["skipped_held"]` 计数；`tests/test_portfolio_position_guard.py` 两例（守卫生效 + 现金流对账：期末现金 = 本金 − 买入成本 + 卖出所得，且买入次数 = 平仓笔数 + 期末持仓），**禁用守卫时两例均失败（已实测）**；测试套件 24 passed。
- **教训**：净值异常时先做**现金流对账**（买入次数 vs 平仓笔数、Σ流水 vs 期末现金），再看策略结论——单看胜率/均亏与净值不匹配就是记账问题的信号。

**"合并即污染"第 4 次坐实（RPS95, 1Y）**：RPS 单跑 -0.6%（282 笔）→ 加四策略联合 **-2.6%**（695 笔），回撤 -9.7% → -12.4%。

### 6.2 ⚠️ 参数默认值单一来源（2026-09-17 修复，A/B 实测踩坑）

**坑**：策略类属性（`RpsBreakoutStrategy.rps_threshold`）与 `compute_events(rps_threshold=90)` / `run_backtest` / argparse **三处各自硬编码默认值**，且 `compute_events` 会 `setattr` 覆盖类属性 → **只改策略类默认值，对回测/组合不生效**。A/B 首次实测即撞上：注入类属性 95 后，90 与 95 结果**完全相同**（-16.3%/392 笔），才发现注入无效。

**修法**：`compute_events` / `run_backtest` / argparse 的 `turtle_window`、`rps_period`、`rps_threshold` 默认一律改 **`None` = 沿用策略类默认属性**（唯一权威），仅显式传参才覆盖（网格 `_run_grid` 仍显式传值，不受影响）。

**验收**：未传参 1275 信号 == 显式 95 的 1275；显式 90 得 2349（≠95）→ 证明"沿用类默认"与"显式覆盖"都正确。`tests/` 15 passed。

**要改策略参数时**：先 `grep -rn "<参数名>" --include="*.py" .` 查全部副本，改完必做"未传参 vs 显式传参"对照验证——别假定改一处就生效。

**流动性分层（新发现，仅作 quality 因子候选，勿硬过滤）**：按成交额四分位，1Y 单调性明显 —— 低额段普遍更好、Q4(最高)最差；**海龟 51% / 均线 48% 的信号落在最差 Q4**。⚠️ 三条不宜硬过滤的理由：① 回测 panel **无历史市值**，用成交额近似实盘的 50-800 亿市值条件不等价；② 低成交额=实盘滑点大；③ 涨停洗盘天然集中在大成交额股，过滤等于砍掉策略本体。可落地形态 = quality 分加"大成交额惩罚"因子。

**高窄旗形不再投入**：1Y 仅 294 信号，95%CI [42.0, 53.3]（宽 11pp）无法判断，非可靠 alpha 源。


### 6.3 出场规则 / 入场跳空 / 批内错峰 三项验证（2026-09-18）

**① 出场规则对比（RPS95、1Y、同参数只换 exit）—— hold=10 最优，不用固定止损**

| 出场规则 | 净收益 | 回撤 | 笔数 | 胜率 | 均持 | 触发分布 |
|---|---|---|---|---|---|---|
| time hold=20 | -3.3% | -12.6% | 263 | 43.7% | 29.0 | time-20d 251 |
| **time hold=10** | **+1.9%** | **-7.2%** | 346 | **44.5%** | 14.9 | time-10d 346 |
| stop-8% hold=20 | +0.2% | -8.8% | 329 | **29.8%** | 16.4 | stop 214 + time 109 |
| chandelier hold=20 | -0.6% | -9.7% | 282 | 42.2% | 23.6 | time 141 + chand 134 |
| **chandelier hold=10** | **+2.2%** | **-7.2%** | 349 | **44.1%** | 14.1 | time 285 + chand 64 |

三条：① **缩短持有期 > 加止损**；② **固定止损 -8% 是陷阱**——净收益看着改善（+0.2%），但胜率暴跌至 29.8%、65% 仓位（214/329）被震出，靠少数大赢家撑住；③ 吊灯只覆盖 18%（64/349），hold=10 下与纯时间几乎无差（+2.2% vs +1.9%）。
→ 采用 **time-10d + 吊灯辅助，不用固定止损**。
校验：E4/E5 与 09-17 A/B 逐位一致（-0.6%/-9.7%/282笔/42.2% 与 +2.2%/-7.2%/349笔/44.1%）→ 口径对齐通过。

**② 入场跳空过滤 —— ❌ 不成立（负结论，不做硬过滤）**

原假设（8 条模拟持仓观察：浮亏最重 3 只 600865/000978/000759 全是高开 +5.7~8.8% 追进）：次日开盘高开 ≥5% 应放弃。

1Y/2Y 事件研究（RPS95，h=10，net，按次日开盘相对前收的跳空分组）：

| 跳空 | 1Y n | 1Y net | 1Y 胜率 | 2Y n | 2Y net | 2Y 胜率 |
|---|---|---|---|---|---|---|
| 低开<0% | 685 | +3.44% | 53.0% | 2418 | +0.82% | 44.6% |
| 0~2% | 300 | +2.93% | 47.7% | 1033 | +0.93% | 45.7% |
| 2~5% | 139 | +0.26% | 47.5% | 475 | +0.74% | 42.7% |
| **5~9.9%** | 56 | **-3.58%** | 39.3% | 212 | **-1.79%** | 38.2% |
| ≥9.9% 涨停板 | 23 | -0.84% | 34.8% | 145 | **+3.27%** | 36.6% |

- 5~9.9% 段**两期同为负**（是真的）但仅占 4.7%/4.9% → 剔除后全样本只改善 1Y +2.53→+2.91%（**+0.38pp**）、2Y +0.79→+0.84%（**+0.05pp**）
- ≥9.9% 段两期**反向**（1Y -0.84% vs 2Y +3.27%）→ 不一致
- 组合层（1Y）阈值敏感性**非单调**：不限 +2.2% → >7% +2.0% → >5% +2.4% → >3% +2.1% → 噪声主导
- 其它策略同样微弱：全部合并 -0.04→+0.01%、海龟 -0.10→-0.05%、均线 -0.19→-0.15%

→ **判定：不做硬过滤**（8 条样本是尾部效应，规模化后 <0.4pp 且阈值非单调；RPS95 自身的 +15.7pp 是它的 40 倍）。
落地形态 = ①日报出一行可执行提示（**跳空只有次日开盘才知道，日报收盘后生成、无法预过滤**）；②本节留档。**注意教训：小样本（8 条）观察到的强效应可能只是尾部，务必在全信号上验证。**

**③ 批内错峰持有期 —— ❌ 无可靠效应（负结论，不做）**

动机：同批买入的持仓同日集中到期（09-04 批 16 只同日）。测试批内持有期错峰（RPS95/1Y/chandelier）：

| 持有期设定 | 净收益 | 回撤 | 胜率 |
|---|---|---|---|
| 统一 10（**取自 ① 的 E5 已验证值**） | +2.2% | -7.2% | 44.1% |
| 错峰 [9,10,11] | +1.8% | -7.1% | 43.2% |
| 错峰 [8,10,12] | +3.7% | -6.3% | 46.7% |
| 错峰 [8,9,10,11,12] | +2.8% | -7.2% | 43.9% |

三个错峰变体**平均持有期都是 10 日**（概念上等价），结果却跨 +1.8~+3.7%（1.9pp）且无单调性 → 路径依赖噪声。
→ **不自动错峰**；改为离场段落输出「⏳ 即将到期」预告清单，交人工分散卖出。

⚠️ **脚本坑（差点得出相反结论，务必记）**：本项测试的 `BASE_KW` **漏传 `hold_days`**，导致标着"统一 hold=10"的对照行**实际跑的是 hold=20**（输出 `退出分布{'time-20d': 141,...}`、均持 23.6 日即铁证），得 -0.6%。若误把它当 hold=10 基线，错峰组看起来能加 **+2.4~+4.3pp** —— 那是把「持有期 20→10」这个**已知效应误算成「错峰」的功劳**。表中 `+2.2%` 是取自 ① 的 E5 已验证值。
**教训**：参数化 A/B 脚本必须**显式传全部参数**（勿依赖默认值），且**先核对对照行是否等于一个已知结果**——本例中 -0.6%/282笔/23.6日 与 hold=20 完全吻合，本该一眼识破。

**④ 新增功能：方案 A 离场提示（`tracker.py`）**

- `exit_signals(state)`：按个股合并持仓（123 条 → 85 只），算 ①到期（满 `EXIT_HOLD_DAYS=10` 交易日）②吊灯止损（收盘 < 峰值−`EXIT_CHANDELIER_K=3.0`×ATR14）
- `report()` 新增「🚪 离场提示」段：建议离场清单（含到期日）+ ⏸ 继续持有数与浮亏最大 3 只 + ⏳ 即将到期（1~2 日内）预告
- `_load_names()`：代码→名称缓存 `data/stock_names.json`（baostock `query_stock_basic` 全量 8829 只，TTL 7 天）——**库内 `stock_daily` 不存股票名**，必须缓存否则每次日报都得联网拉全量；刷新失败退回旧缓存（哪怕过期），无缓存返回空 dict
- `_shift_trade_days()`：把起始日算作第 1 个交易日取第 n 日（即 `d >= after`；**首版误用 `d > after` 导致到期日差一天**）→ 算到期日；**注意未来交易日无行情数据、算不出具体日期**，故"即将到期"用"还需 N 日"表述
- ⚠️ 输出已自带口径提醒：方案 A 是模拟盘（每批独立 100 万名义本金、100 股整手），**非用户实盘持仓，需自行对照**
- CLI `report` 与 `update` 两条路径都要传 `exit_rows`/`names`（首版漏了 CLI 分支，实测发现离场段不显示）


### 6.4 ⚠️ 交付链路自检（2026-09-20）：功能"上线"≠送达

对 14 个交易日 cron 输出做体检，发现两个真问题 + 一个预期报错被误报。**教训：改完脚本必须验证"用户实际收到了什么"，不能只看本地能跑。**

**① 离场提示从未送达（P0，双重缺口）**

`exit_signals` 实现、本地验证、推送都做了，但翻遍 cron 输出正文 `离场` 出现 **0 次**。两处独立缺口叠加：

- **prompt 缺口**：cron 任务 prompt 的 6 步报告结构里**没有「离场」这一步** → 即使 script 输出有清单，agent 也不会写进日报
- **触发缺口**：2026-09-18 那次 wrapper 短路（见 ②），**tracker 步骤根本没跑**（铁证：`track_state.json` mtime 停在当日 04:11，而日报是 13:39）

修复：① wrapper 第 7 步**把离场清单直接注入结构化摘要**（不再只依赖 tracker 长输出段）；② cron prompt 新增第 6 步「🚪 离场提示（必带小节）」。

**② `NO_TRADING_DAY` 把"数据未发布"误判为"非交易日"（P1）**

2026-09-18 实况：20:00 触发 → `main.py` 晚间慢跑约 48 分钟 → 20:49 同步完仍只有 09-17 数据（baostock 当日 K 到 **~21:00** 才发布）→ 守卫判 `NO_TRADING_DAY` → **整条流水线短路**（backtest/report/tracker 全跳过）→ 靠 cron agent 临场复查、21:02 重新同步才补上（日报晚 1h40m，且漏 tracker）。

根因：守卫只比「库内最新数据日」与「日历最近已收盘日」，**无法区分真休市与数据延迟**；且旧日历回退只跳周末、不识别节假日。

修复（`sequoia_x/trading_calendar.py` 新增 + wrapper 重构）：

- 用 baostock 权威日历 `query_trade_dates` 判「是否交易日」（节假日也算得准），失败才回退旧周末推算
- 日历检查**提到同步之前** —— 避免数据未发布时白跑 48 分钟全市场同步
- 交易日且数据未发布 → 探针轮询等待（`query_history_k_data_plus` 查 `sh.600000` 等 4 只样本的当日收盘价，`SEQUOIA_PROBE_WAIT=600` × `SEQUOIA_PROBE_MAX=8` 最多 80 分钟）→ 仍无 → **新标志 `DATA_NOT_READY`**（明确告知不是非交易日）
- 同步后二次校验兜底：数据日仍未达 → `DATA_NOT_READY`
- 非交易日：数据已是最新交易日才 `NO_TRADING_DAY`；数据落后则继续**补跑**（避免非交易日一律短路丢失自愈机会）；手动重跑同日报告用 `SEQUOIA_FORCE=1`
- 超时预算：最坏路径 = 80(探针等待) + 60(同步) + 20(回测/报告/跟踪) ≈ **160 分钟** → `cron.script_timeout_seconds` 由 7200 提到 **10800**（0.19s/只→0.56s/只 的晚间波动也要留量）

**③ 仓库自带飞书推送的 `[19001]` 是预期行为（P2）**

`.env` 的 `FEISHU_WEBHOOK_URL` 是占位值 → `main.py` 推送必报 `webhook access token invalid`。这是设计如此（投递走 Hermes 通道），但 2026-09-18 的 agent 把它当异常写进了日报末尾。已在 cron prompt 加「不要报告的事」段明确禁止。⚠️ **占位值不得外泄、不得换成真实 token**。

**④ 观察：状态机建议与正期望策略分歧**

`regime=down_low` 建议回避「海龟突破、RPS 突破」，但回测里 **RPS 是唯一正期望策略**（10 日 +2.09%）。建议方向与数据方向相反，日报里保留这个分歧提示供人工判断。

**自查清单（改脚本后必跑）**：

```bash
# ① 标志分支：改 wrapper 后至少验证「非交易日」与「数据未就绪」两条短路路径
bash scripts/sequoia_report.sh                      # 周末应秒出 NO_TRADING_DAY
python -m sequoia_x.trading_calendar --recent-closed  # 日历可用性
python -m sequoia_x.trading_calendar --probe 2026-09-18  # 探针（应 PUBLISHED=1）
# ② 摘要注入：确认离场段真的进了最终输出（不是只在 tracker 长段里）
SEQUOIA_FORCE=1 SEQUOIA_SKIP_SYNC=1 bash scripts/sequoia_report.sh | tail -30
# ③ 送达验证：次日翻 cron 输出文件，确认新段真的出现（本项就是漏掉才出的事）
grep -c 离场 /opt/data/cron/output/<job_id>/<最新>.md
```

## 7. 运维

- **cron `0fccfa4dd1f7`**：交易日北京 20:00（`0 12 * * 1-5` UTC，勿改盘前——数据日≠今日会判数据未就绪）；deliver=origin（定时 tick 投递可靠，agent.log 有 `delivered ... via live adapter` 铁证）；script=`/opt/data/scripts/sequoia_report.sh`（日历/数据就绪 → sync容错 → 回测缓存刷新 → report → tracker → 摘要含离场）
- **`cron.script_timeout_seconds = 10800`**（2026-09-20 由 7200 提高）：最坏路径含 80 分钟探针等待 + 60 分钟同步 ≈ 160 分钟。⚠️ 实时读取，改后无需重启 gateway
- **wrapper 标志分支**：`NO_TRADING_DAY`（日历判定真休市）｜`DATA_NOT_READY`（交易日但 baostock 数据未发布 —— **不是非交易日**）｜`SEQUOIA_ERROR`。cron prompt 已按三者分别指示
- ⚠️ **/opt/data/scripts/sequoia_report.sh 必须是真文件**（薄启动器 `exec bash .../scripts/sequoia_report.sh`，主逻辑在仓库内维护）——2026-09-04 首日正式 tick 曾因该路径是**指向仓库的符号链接**被 cron runner realpath 校验拦截（`Blocked: script path resolves outside the scripts directory`），wrapper 完全没跑；force 版同坑排查
- wrapper `/opt/data/scripts/sequoia_report.sh`：sync timeout 3600 容错(exit=124 降级；2026-09-15 由 1800 上调——当日晚间 baostock 实测 0.56s/只≈48.5min，超时截杀致 engine 0 行落库，真交易日被误判 NO_TRADING_DAY) → backtest 1y 刷新缓存(失败沿用旧) → report → 摘要注入 agent；`SEQUOIA_FORCE=1` 跳过交易日检测（跨日验证）；防呆"滤后>0但区间内全0"→60s重试
- ⚠️ wrapper 交易日检测 python 必须前缀 `TZ=Asia/Shanghai`（容器本地=UTC，naive now 按 UTC 恒判"未收盘"，sync 失败时会用旧数据日出报告而非 NO_TRADING_DAY；2026-09-04 790aa33 修复）
- ⚠️ 手动 `cronjob run` **不投递**到 chat（走 delegation，自报 delivered 是假的）——验证投递用一次性定时 job 或等正式 tick
- 僵尸 running 记录卡防重入：`UPDATE executions SET status='completed' WHERE status='running'`（/opt/data/cron/executions.db）
- wrapper 禁止并发（多实例 sync 互踩曾删库——engine 已改 symbol+date 精确删除）

## 8. 开发纪律

- 开发在 **dev 分支**（fork hacperme/Sequoia-X，跟踪 origin/dev）；commit 本地 → 用户确认后 push
- 只 `git add` 功能文件（sequoia_x/*、main.py）；辅助脚本(data/*.json、*.py 工具)与 data/ 不入库
- git 身份：hacper / **git@hacperme.com**（2026-09-04 起，全局+仓库级）
- 每个功能"实现+测试验证"通过才提交；测试先行（**`pytest tests/` 15 项**；全策略冒烟跑 `python -m sequoia_x.report --markdown`）
- 提交信息中文要点式，标注实测结论与踩坑
- **同步更新本 SKILL.md**（本文档与代码同版本演进；新增功能/新研究结论/新坑必记）
